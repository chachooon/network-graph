interface Props {
  item: any;
}

export default function TableHeadItem({ item }: Props) {
  //   const id = useId();

  //   const spring = useSpring({
  //     width: checked ? 24 : 0,
  //   });

  return null;
}
