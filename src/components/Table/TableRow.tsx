interface Props {
  data: any;
}

export default function TableRow({ data }: Props) {
  //   const id = useId();

  //   const spring = useSpring({
  //     width: checked ? 24 : 0,
  //   });

  return null;
}
